<main>
    # Jogos

    {{#if games}}
        Estes são os últimos 15 jogos para o estado `{{status}}`. {{#if finished}}O horário de certos jogos poderá não estar definido, mas irá ser atualizado.{{/if}}
        {{#each games}}
            - {{this}}{{/each}}
    {{else}}
        Não há jogos para o estado `{{status}}`.
    {{/if}}
</main>
<button id="previous-page" class="primary"><</button>
<button id="next-page"  class="primary">></button>